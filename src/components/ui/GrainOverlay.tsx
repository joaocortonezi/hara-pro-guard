/**
 * Fixed full-screen SVG noise overlay (2-3% opacity).
 * Adds tactile, physical quality per design system spec.
 */
export function GrainOverlay() {
  return <div aria-hidden className="grain-overlay" />;
}
